# Gender-Classifier-using-Neural-Network
A Gender Classification project using a Convolutional Neural Network (CNN) built with TensorFlow and Keras. Trained on the CelebA dataset (200k+ images), the model features a 4-layer Conv2D architecture with MaxPooling and Dropout for robust feature extraction.
